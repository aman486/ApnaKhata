class Payment{
    constructor(userid, name,advance,borrow){
        this.userid = userid;
        this.name = name;
        this.advance = advance;
        this.borrow = borrow;
    }
}

export default Payment;